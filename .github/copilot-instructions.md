# Copilot Instructions

## 项目指南
- Preferences: language Chinese; preferred terminal shell is powershell.exe; small feature changes don't require immediate git commit but must compile before committing; when deleting files use Microsoft.VisualBasic.FileIO.FileSystem DeleteFile/DeleteDirectory to send to Recycle Bin; git commit messages follow Conventional Commits format (type: description).